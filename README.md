# Circuit
Wokwi.com 
setup: Arduino Uno, an ultrasonic distance sensor, a small servo motor 
program: reads physical sensor input. If an object is detected within a specific centimeter range, the microcontroller must trigger the motor to rotate exactly 90 degrees, hold for five seconds, and close. and a simple emergency stop button using manual hardware wiring rules.
